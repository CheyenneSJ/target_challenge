name: target_challenge
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.6.0
  intl: ^0.18.1
  mobx: ^2.2.1
  flutter_mobx: ^2.1.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  validatorless: ^1.2.3
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  mobx_codegen: ^2.4.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: 'assets/images/logo.jpeg' 

flutter:
  uses-material-design: true

  assets:
    - assets/images/
